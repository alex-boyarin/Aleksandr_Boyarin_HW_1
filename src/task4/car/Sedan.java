package task4.car;

public class Sedan extends Car {
    public Sedan(String typeBody, String brand, String modelCar, String typeFuel, double fuelConsumption, int maxSpeed, double price) {
        super(typeBody, brand, modelCar, typeFuel, fuelConsumption, maxSpeed, price);
    }
}
